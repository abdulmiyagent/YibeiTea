# Database (Supabase)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Mollie Payments
MOLLIE_API_KEY=""
MOLLIE_PROFILE_ID=""

# Brevo Email (EU-based, GDPR compliant)
BREVO_API_KEY=""
BREVO_FROM_EMAIL="noreply@yibeitea.be"

# Cloudinary (optional - for product image uploads in admin panel)
# If not configured, image uploads use local storage (public/images folder)
# Note: Local storage works but images won't persist after Vercel redeployments
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Upstash Redis (for rate limiting) - Optional, falls back to in-memory for local dev
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Mailtrap (for e2e email testing) - Optional
# API credentials (for reading emails in tests)
MAILTRAP_API_TOKEN=""
MAILTRAP_ACCOUNT_ID=""
MAILTRAP_INBOX_ID=""
# SMTP credentials (for sending emails via Mailtrap instead of Brevo)
# When these are set, all emails go to Mailtrap inbox instead of real recipients
MAILTRAP_SMTP_USER=""
MAILTRAP_SMTP_PASS=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
