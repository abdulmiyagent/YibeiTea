// Default export for @modal slot - renders nothing when no modal is active
// This is required by Next.js parallel routes to have a default state
export default function ModalDefault() {
  return null;
}
